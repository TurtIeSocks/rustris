use bevy::prelude::*;

pub mod board;
pub mod lines;
pub mod next_piece;
pub mod score;
mod stats;

// pub trait UI {
//     fn setup(&mut self, commands: &mut Commands);
//     fn reset(&mut self, commands: &mut Commands);
//     fn update(&mut self, commands: &mut Commands);
// }
